---
phase: 05-chat-format-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/generator/generator.go
  - internal/generator/generator_test.go
  - internal/templates/templates.go
  - internal/templates/templates_test.go
  - internal/templates/prompts/chat_train.tmpl
  - internal/templates/prompts/chat_valid.tmpl
  - internal/templates/prompts/chat_test.tmpl
autonomous: true

must_haves:
  truths:
    - "FormatType enum with 'completion' and 'chat' values exists and validates correctly"
    - "Chat templates instruct LLM to generate single-turn conversations with context-free user messages and style-matching assistant responses"
    - "Generator selects correct template based on format type and split type"
    - "Generator parses chat-format LLM responses (user/assistant JSON) into Pair structs"
  artifacts:
    - path: "internal/generator/generator.go"
      provides: "FormatType enum, ParseFormat(), format-aware getTemplateName(), parseChatResponse()"
      contains: "FormatType"
    - path: "internal/templates/prompts/chat_train.tmpl"
      provides: "Chat training template"
      contains: "single-turn"
    - path: "internal/templates/prompts/chat_valid.tmpl"
      provides: "Chat validation template"
      contains: "single-turn"
    - path: "internal/templates/prompts/chat_test.tmpl"
      provides: "Chat test template"
      contains: "single-turn"
  key_links:
    - from: "internal/generator/generator.go"
      to: "internal/templates/prompts/chat_*.tmpl"
      via: "getTemplateName returns chat_train.tmpl etc when format is chat"
      pattern: "chat_.*\\.tmpl"
---

<objective>
Add FormatType enum, chat prompt templates, and format-aware generator logic for chat-style training data generation.

Purpose: Establishes the foundational types and templates needed for chat format support. Without these, the pipeline cannot select chat templates or parse chat-format LLM responses.
Output: FormatType enum with validation, three chat templates (train/valid/test), format-aware template selection, and chat response parsing in the generator.
</objective>

<execution_context>
@/Users/dan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-chat-format-support/05-CONTEXT.md
@.planning/phases/05-chat-format-support/05-RESEARCH.md
@internal/generator/generator.go
@internal/generator/generator_test.go
@internal/templates/templates.go
@internal/templates/templates_test.go
@internal/templates/prompts/train.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FormatType enum and format-aware template selection</name>
  <files>
    internal/generator/generator.go
    internal/generator/generator_test.go
  </files>
  <action>
Add FormatType string enum to generator.go following the existing SplitType pattern:

```go
type FormatType string

const (
    FormatCompletion FormatType = "completion"
    FormatChat       FormatType = "chat"
)

func ParseFormat(s string) (FormatType, error) {
    switch s {
    case string(FormatCompletion):
        return FormatCompletion, nil
    case string(FormatChat):
        return FormatChat, nil
    default:
        return "", fmt.Errorf("invalid format: %s (must be 'completion' or 'chat')", s)
    }
}
```

Update `getTemplateName` to accept format type. Change signature to `getTemplateName(format FormatType, split SplitType) string`. For FormatCompletion, return existing names (train.tmpl, valid.tmpl, test.tmpl). For FormatChat, return chat_train.tmpl, chat_valid.tmpl, chat_test.tmpl. Default falls back to completion format.

Update all callers of `getTemplateName` in generator.go to pass format. Add a `Format` field to the `Config` struct (type FormatType) with default value `FormatCompletion` in `DefaultConfig()`. The `Generate()` method should use `g.config.Format` when calling `getTemplateName`.

Add `parseChatResponse(response string) []Pair` method that parses LLM responses in chat format. The LLM will return JSON like:
```json
[{"user": "question", "assistant": "answer"}]
```
This method extracts JSON array, unmarshals into a temporary struct with User/Assistant fields, then converts to []Pair (User->Prompt, Assistant->Completion). This reuses the existing Pair struct so deduplication works unchanged.

Create a helper struct for chat parsing:
```go
type chatPair struct {
    User      string `json:"user"`
    Assistant string `json:"assistant"`
}
```

In `Generate()`, after calling the provider, select the appropriate parser based on format:
- FormatCompletion: use existing `parseResponse()`
- FormatChat: use `parseChatResponse()`

Add tests in generator_test.go:
- TestParseFormat: valid inputs ("completion", "chat"), invalid input ("json"), empty string
- TestGetTemplateName updated to include format parameter tests for both completion and chat
- TestParseChatResponse: valid single pair, valid multiple pairs, extra text before/after JSON, empty response, malformed JSON, empty array
  </action>
  <verify>
Run `make lint && make test` -- all tests pass including new FormatType and chat parsing tests.
  </verify>
  <done>
FormatType enum exists with ParseFormat validation. getTemplateName selects chat templates when format is chat. parseChatResponse correctly parses LLM chat-format responses into Pair structs. All existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create chat prompt templates</name>
  <files>
    internal/templates/prompts/chat_train.tmpl
    internal/templates/prompts/chat_valid.tmpl
    internal/templates/prompts/chat_test.tmpl
    internal/templates/templates_test.go
  </files>
  <action>
Create three chat templates following the locked decisions from CONTEXT.md:

**chat_train.tmpl:**
```
You are generating high-quality chat training data for an LLM.

Given the documentation below, generate exactly {{.PairCount}} single-turn conversations.

Each conversation consists of ONE user message and ONE assistant response.

CRITICAL RULES:
- User messages must be context-free: do NOT reference "the document", "the guide", "this text", or any source material
- Questions should stand alone as if asked by someone with no knowledge of the source
- Assistant responses must mirror the voice, tone, dialect, and conversational style of the source document
- If the document is casual, use casual language. If formal, use formal language. Match the author's speaking patterns.
- Mix question styles for diversity:
  - Factual: "What is X?", "How does Y work?"
  - Clarifying: "Why would someone use Z?", "What's the difference between A and B?"
  - How-to: "How do I configure X?", "What steps are needed for Y?"
  - Conceptual: "What are the tradeoffs of X?", "When should I avoid Y?"

Output format:
Return ONLY a JSON array of objects in this exact form:
[
  {"user": "<context-free question>", "assistant": "<response in document's voice>"}
]

{{if .ExcludePairs}}
IMPORTANT:
Do NOT generate questions semantically similar to these previously generated pairs.
Avoid overlapping intent, not just wording.

Previously generated:
{{range .ExcludePairs}}
- User: "{{.Prompt}}"
  Assistant: "{{.Completion}}"
{{end}}
{{end}}

Document:
{{.DocumentContent}}
```

**chat_valid.tmpl:**
Same structure but validation-focused. Change the intro to "You are generating validation chat data for an LLM." Add rules that questions must test deep understanding, not memorisation. Require reasoning across multiple parts of the document, understanding constraints, edge cases, and distinguishing similar concepts. Questions should be harder and more indirect than training examples. Keep all other CRITICAL RULES (context-free, style-matching, question mix). Use same output format and exclude section.

**chat_test.tmpl:**
Same structure but test-focused. Change intro to "You are generating TEST chat data for an LLM." Add rules requiring multi-step reasoning, combining information from multiple document parts, careful handling of constraints/exceptions/edge cases. Questions should be difficult for a model that has only memorised patterns. Prefer questions where a small misunderstanding leads to incorrect answers. Avoid hints, leading phrasing, or restating document language. Keep all other CRITICAL RULES (context-free, style-matching). Use same output format and exclude section.

Add tests in templates_test.go:
- TestExecuteTemplate_chatTrain: verify template renders with document content, pair count, and "single-turn" appears
- TestExecuteTemplate_chatValid: verify template renders with "validation" context
- TestExecuteTemplate_chatTest: verify template renders with "TEST" context
- TestExecuteTemplate_chatExcludePairs: verify exclusion section renders correctly in chat template
- TestExecuteTemplate_chatNoExcludePairs: verify no exclusion section when ExcludePairs is nil/empty
  </action>
  <verify>
Run `make lint && make test` -- all tests pass including new chat template tests. Chat templates are embedded and render correctly.
  </verify>
  <done>
Three chat templates exist (chat_train.tmpl, chat_valid.tmpl, chat_test.tmpl) with context-free user messages, style-matching instructions, question variety, and exclusion support. Templates render correctly via ExecuteTemplate.
  </done>
</task>

</tasks>

<verification>
1. `make lint` passes with no errors
2. `make test` passes with all existing and new tests
3. `go test -v -run TestParseFormat ./internal/generator` passes
4. `go test -v -run TestParseChatResponse ./internal/generator` passes
5. `go test -v -run TestExecuteTemplate_chat ./internal/templates` passes
</verification>

<success_criteria>
- FormatType("completion") and FormatType("chat") are valid; ParseFormat rejects invalid values
- getTemplateName(FormatChat, SplitTrain) returns "chat_train.tmpl"
- parseChatResponse correctly converts {"user":"Q","assistant":"A"} to Pair{Prompt:"Q",Completion:"A"}
- All three chat templates render with DocumentContent, PairCount, and ExcludePairs
- Chat templates include explicit instructions for context-free questions and style-matching responses
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/05-chat-format-support/05-01-SUMMARY.md`
</output>
