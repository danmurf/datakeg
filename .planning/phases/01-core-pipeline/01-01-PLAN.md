---
phase: 01-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/datakeg/main.go
  - cmd/datakeg/commands/root.go
  - cmd/datakeg/commands/generate.go
  - internal/writer/jsonl.go
  - go.mod
  - go.sum
autonomous: true

must_haves:
  truths:
    - "Running `go build ./cmd/datakeg` produces a binary without errors"
    - "Running `datakeg --help` shows available commands including generate"
    - "Running `datakeg generate --help` shows source and output arguments plus flags"
    - "JSONL writer can write TrainingPair structs to files"
  artifacts:
    - path: "cmd/datakeg/main.go"
      provides: "CLI entry point"
      min_lines: 10
    - path: "cmd/datakeg/commands/root.go"
      provides: "Root command definition"
      contains: "rootCmd"
    - path: "cmd/datakeg/commands/generate.go"
      provides: "Generate command with args and flags"
      contains: "generateCmd"
    - path: "internal/writer/jsonl.go"
      provides: "JSONL file writing"
      exports: ["WriteJSONL", "TrainingPair"]
  key_links:
    - from: "cmd/datakeg/main.go"
      to: "cmd/datakeg/commands"
      via: "commands.Execute()"
      pattern: "commands\\.Execute"
    - from: "cmd/datakeg/commands/root.go"
      to: "cmd/datakeg/commands/generate.go"
      via: "rootCmd.AddCommand"
      pattern: "AddCommand.*generateCmd"
---

<objective>
Establish the CLI skeleton and JSONL output capability.

Purpose: Create the foundational structure that all other components will plug into. The CLI must accept `generate <source> <output>` with proper flags, and the JSONL writer must be ready for the pipeline.

Output: Runnable CLI binary with help text, generate command stub, and working JSONL writer.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-core-pipeline/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI scaffold with Cobra</name>
  <files>cmd/datakeg/main.go, cmd/datakeg/commands/root.go, cmd/datakeg/commands/generate.go, go.mod, go.sum</files>
  <action>
1. Update go.mod to add cobra dependency: `go get github.com/spf13/cobra@latest`

2. Create cmd/datakeg/main.go:
   - Import commands package
   - Call commands.Execute() in main()
   - Use os.Exit(1) if Execute returns error

3. Create cmd/datakeg/commands/root.go:
   - Define rootCmd with Use: "datakeg", Short description
   - Add Execute() function that returns rootCmd.Execute()
   - Use init() to add generate command

4. Create cmd/datakeg/commands/generate.go:
   - Define generateCmd with Use: "generate &lt;source&gt; &lt;output&gt;"
   - Args: cobra.ExactArgs(2) for source and output paths
   - Use RunE (not Run) to return errors properly
   - Add flags with sensible defaults (from RESEARCH.md):
     - --model, -m: string, default "gpt-oss:20b"
     - --train-pct: float64, default 0.6
     - --valid-pct: float64, default 0.2
     - --test-pct: float64, default 0.2
     - --pairs-per-1k: float64, default 1.0
   - RunE body: extract args and flags, print placeholder message, return nil

Follow patterns from RESEARCH.md Pattern 1 (Cobra Command Structure).
  </action>
  <verify>
Run: `cd /Users/dan/code/datakeg && go build ./cmd/datakeg && ./datakeg --help && ./datakeg generate --help`
Expected: Help text shows datakeg commands and generate subcommand with all flags.
  </verify>
  <done>
- `go build ./cmd/datakeg` succeeds
- `./datakeg --help` shows "generate" in commands list
- `./datakeg generate --help` shows source/output args and all 5 flags with defaults
  </done>
</task>

<task type="auto">
  <name>Task 2: Create JSONL writer</name>
  <files>internal/writer/jsonl.go</files>
  <action>
Create internal/writer/jsonl.go with:

1. TrainingPair struct:
   - Prompt string `json:"prompt"`
   - Completion string `json:"completion"`

2. WriteJSONL function:
   - Signature: func WriteJSONL(filename string, pairs []TrainingPair) error
   - Create file with os.Create
   - defer file.Close() immediately after success check
   - Use json.NewEncoder(file) for streaming writes
   - Loop through pairs, call encoder.Encode() for each
   - Check and wrap errors with fmt.Errorf for context
   - Call file.Sync() before return to ensure data is flushed

Follow patterns from RESEARCH.md Pattern 3 (JSONL Encoding with json.Encoder).
Do NOT use json.Marshal + manual newlines - Encoder.Encode() adds newlines automatically.
  </action>
  <verify>
Create a simple test: add internal/writer/jsonl_test.go with a test that writes two pairs to a temp file and reads it back to verify format.
Run: `cd /Users/dan/code/datakeg && go test ./internal/writer/...`
  </verify>
  <done>
- internal/writer/jsonl.go exists with TrainingPair and WriteJSONL
- Test passes confirming JSONL format is correct (one JSON object per line)
- Each line in output is valid JSON with "prompt" and "completion" keys
  </done>
</task>

</tasks>

<verification>
1. `go build ./cmd/datakeg` - compiles without errors
2. `./datakeg --help` - shows root help with generate command listed
3. `./datakeg generate --help` - shows generate help with args and all flags
4. `./datakeg generate /tmp/src /tmp/out` - runs without error (placeholder behavior)
5. `go test ./internal/writer/...` - JSONL writer tests pass
</verification>

<success_criteria>
- CLI binary builds and runs
- Generate command accepts source and output args
- All 5 config flags present with defaults (model, train-pct, valid-pct, test-pct, pairs-per-1k)
- JSONL writer produces valid JSONL format (one JSON object per line)
- No compile errors or test failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md`
</output>
