---
phase: 03-production-ready
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "cmd/datakeg/main.go"
  - "cmd/datakeg/commands/generate.go"
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can run datakeg generate --skip-merge and get per-document files only"
    - "Per-document files follow naming pattern {docname}_train.jsonl, {docname}_valid.jsonl, {docname}_test.jsonl"
    - "Merge step can be skipped entirely when --skip-merge is used"
  artifacts:
    - path: "cmd/datakeg/main.go"
      provides: "--skip-merge flag addition"
    - path: "cmd/datakeg/commands/generate.go"
      provides: "Per-document file output and conditional merge"
  key_links:
    - from: "cmd/datakeg/main.go"
      to: "cmd/datakeg/commands/generate.go"
      via: "skipMerge parameter"
---

<objective>
Add per-document JSONL file output and --skip-merge flag to the generate command.

**Purpose:** Phase 3 starts by enabling granular output (IO-05) and the --skip-merge option (CLI-02). This allows users to inspect per-document outputs before merging, and enables the merge subcommand to be used separately.

**Output:** Modified generate command that writes per-document files and supports --skip-merge flag.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

# Reference: Current pipeline structure
@cmd/datakeg/commands/generate.go
@cmd/datakeg/main.go
</context>

<tasks>

<task type="auto">
  <name>Add --skip-merge flag to CLI</name>
  <files>cmd/datakeg/main.go</files>
  <action>
Add a new boolean flag `flagSkipMerge` to the generate command configuration in `cmd/datakeg/main.go`:

1. Add `flagSkipMerge` variable declaration alongside existing flag variables
2. Add the flag registration: `generateCmd.Flags().BoolVarP(&flagSkipMerge, "skip-merge", "", false, "Skip merging per-document files into master files")`
3. Pass the flag value to `ExecuteGeneratePipeline` as a new parameter

**Changes:**
- Add `flagSkipMerge bool` to the flag variables section (around line 32-39)
- Add flag registration in `init()` function (around line 66-71)
- Update `runGenerate` function signature call to pass the new parameter
</action>
  <verify>grep -n "skip-merge" cmd/datakeg/main.go && grep -n "flagSkipMerge" cmd/datakeg/main.go</verify>
  <done>--skip-merge flag exists in generate command help output (`datakeg generate --help`)</done>
</task>

<task type="auto">
  <name>Modify pipeline to write per-document files</name>
  <files>cmd/datakeg/commands/generate.go</files>
  <action>
Refactor `ExecuteGeneratePipeline` to write per-document JSONL files during processing:

**Changes to cmd/datakeg/commands/generate.go:**

1. Add `skipMerge bool` parameter to function signature
2. Modify the document processing loop to write per-document files immediately after generating pairs:
   - Create per-document output filename using pattern: `{docname}_train.jsonl`, `{docname}_valid.jsonl`, `{docname}_test.jsonl`
   - Use `writer.WriteJSONL` for each non-empty split
   - Filename: sanitize document name (remove .md/.txt extension, replace spaces with underscores)
   - Path: write to the same outputDir as master files would go

3. Keep existing global trainPairs/validPairs/testPairs accumulation for potential merge step
4. Conditionally skip the master file merge step when `skipMerge == true`

**Per-document filename logic:**
```go
docName := filepath.Base(doc.Path)
ext := filepath.Ext(docName)
docName = docName[:len(docName)-len(ext)] // Remove extension
docName = strings.ReplaceAll(docName, " ", "_") // Sanitize spaces
perDocFile := filepath.Join(outputDir, docName+"_train.jsonl")
```

5. Update the call to `writer.WriteJSONL` to use per-document filenames

**Error handling:** Return descriptive errors if per-document file write fails.
</action>
  <verify>grep -n "_train.jsonl" cmd/datakeg/commands/generate.go && grep -n "skipMerge" cmd/datakeg/commands/generate.go</verify>
  <done>Pipeline writes per-document files during processing and respects --skip-merge flag</done>
</task>

<task type="auto">
  <name>Update writer to support per-document paths</name>
  <files>internal/writer/jsonl.go</files>
  <action>
Verify `writer.WriteJSONL` accepts file path (not just filename) and supports the per-document use case.

**Current state check:** The existing `WriteJSONL` function should already accept a full path. If not, modify it to support writing to any specified filepath.

**No changes needed** if `WriteJSONL(outFile, trainPairs)` works with full paths. Confirm by checking that `filepath.Join` is used correctly for per-document filenames.
</action>
  <verify>grep -n "WriteJSONL" internal/writer/jsonl_test.go | head -5</verify>
  <done>writer.WriteJSONL supports full file paths for per-document outputs</done>
</task>

</tasks>

<verification>
Run `datakeg generate --help` and verify:
- `--skip-merge` flag appears in help output
- All other flags remain unchanged

Test the modified pipeline:
1. Create a test directory with sample .md files
2. Run `datakeg generate <source> <output> --skip-merge`
3. Verify per-document files exist: `ls <output>/*_train.jsonl`
4. Verify NO master files exist: `ls <output>/train.jsonl` should not exist
5. Run without --skip-merge flag and verify both per-document AND master files exist
</verification>

<success_criteria>
- [ ] `--skip-merge` flag added to generate command
- [ ] Per-document files written during processing
- [ ] Per-document files follow naming pattern `{docname}_{split}.jsonl`
- [ ] When --skip-merge=true: only per-document files exist
- [ ] When --skip-merge=false: both per-document and master files exist
- [ ] All existing functionality (splits, pairs per 1k, model selection) still works
</success_criteria>

<output>
After completion, create `.planning/phases/03-production-ready/03-01-SUMMARY.md`
</output>
