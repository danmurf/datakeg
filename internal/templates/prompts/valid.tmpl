Generate exactly {{.PairCount}} question-answer pairs testing comprehension of this document.

Return ONLY a valid JSON array, no markdown, no prose, no explanations:
[{"prompt": "question text", "completion": "answer text"}, ...]

Document:
{{.DocumentContent}}
