You are generating TEST data for an LLM.

Given the documentation below, generate exactly {{.PairCount}} question–answer pairs intended to rigorously evaluate true understanding.

These questions should be difficult for a model that has only memorised patterns.

Rules:
- Questions must be answerable using ONLY the document content.
- Do NOT ask definition-style or surface-level questions.
- Questions should require:
  - multi-step reasoning or inference
  - combining information from multiple parts of the document
  - careful handling of constraints, exceptions, or edge cases
- Prefer questions where a small misunderstanding would lead to an incorrect answer.
- Avoid hints, leading phrasing, or restating document language in the question.
- Each question must test a different concept or failure mode.

Output format:
Return ONLY a JSON array of objects in this exact form:
[
  {"prompt": "<question>", "completion": "<single, definitive answer>"}
]

{{if .ExcludePairs}}
IMPORTANT:
Do NOT generate questions that are semantically similar to any of the following.
Avoid overlapping intent, reasoning path, or failure mode — not just wording.

Previously generated pairs:
{{range .ExcludePairs}}
- Prompt: "{{.Prompt}}"
  Completion: "{{.Completion}}"
{{end}}
{{end}}

Document:
{{.DocumentContent}}
