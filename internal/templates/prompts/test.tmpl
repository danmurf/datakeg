Generate exactly {{.PairCount}} challenging question-answer pairs for evaluation of this document.

Return ONLY a valid JSON array, no markdown, no prose, no explanations:
[{"prompt": "question text", "completion": "answer text"}, ...]

Document:
{{.DocumentContent}}
