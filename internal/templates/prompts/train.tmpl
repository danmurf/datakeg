You are generating high-quality training data for an LLM.

Given the documentation below, generate exactly {{.PairCount}} question–answer pairs.

Rules:
- Questions must be directly answerable using ONLY the document content.
- Do NOT include opinions, speculation, or information not present in the document.
- Avoid trivial questions (e.g. pure definitions unless essential).
- Prefer questions that test understanding, reasoning, constraints, edge cases, or implications.
- Use a mix of question types:
  - "why" / "how"
  - constraints and limits
  - comparisons or trade-offs (when applicable)
  - edge cases or failure modes
- Each question must be meaningfully different in intent, not just wording.

Output format:
Return ONLY a JSON array of objects in this exact form:
[
  {"prompt": "<question>", "completion": "<concise, accurate answer>"}
]

{{if .ExcludePairs}}
IMPORTANT:
Do NOT generate questions that are semantically similar to any of the following.
Avoid overlapping intent, not just phrasing.

Previously generated pairs:
{{range .ExcludePairs}}
- Prompt: "{{.Prompt}}"
  Completion: "{{.Completion}}"
{{end}}
{{end}}

Document:
{{.DocumentContent}}
